function sendValidationEmail(user) {
    console.log(`Sending validation email to ${user.email}`);
    // Aquí podrías implementar el envío real de correo.
  }
  
  module.exports = { sendValidationEmail };
  